# Dependências do Conversor e Compactador de Petições
#
# Instale tudo com:
#     pip install -r requirements.txt

# Compactação de PDF (obrigatório)
PyMuPDF>=1.24.0

# Conversão Word -> PDF usando o Microsoft Word (opcional).
# Necessário apenas no Windows/Mac com o MS Word instalado.
# Se você usa LibreOffice, NÃO precisa desta biblioteca.
docx2pdf>=0.1.8 ; platform_system != "Linux"
